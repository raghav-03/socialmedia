       <img src="<%= porfile_user.avatar %>" width="100">
       <% if(user.id == porfile_user.id){%>
                <form action="/user/update/<%=porfile_user.id%>" enctype="multipart/form-data" method="POST">
                <input type="text" name="name" placeholder="Your Name"  value="<%=porfile_user.name%>" required>
                <input type="email" name="email" placeholder="Your E-Mail"  value="<%=porfile_user.email%>" required>
                <input type="file" name="avatar" placeholder="Profile Picture">
                <button type="submit">Update</button>
                </form>
                <br>
        <%}else{%>
                <p>
                        <%=porfile_user.name%>
                </p>
                <p>
                        <%=porfile_user.email%>
                </p>
        <%}%>   